_format_version: "3.0"
_transform: true

services:
  - name: flight-admin-service
    url: http://flight-admin-service:8080

    routes:
      - name: flight-admin-route
        paths:
          - /api/v1/admin
        strip_path: false